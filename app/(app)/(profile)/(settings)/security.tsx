import { Spacer } from "@/components/Base/Spacer"
import { SettingsWrapper } from "@/components/Wrappers/SettingsWrapper"

export default function Security() {
    return (
        <SettingsWrapper header="Security">
            <Spacer height={70}/>
        </SettingsWrapper>
    )
}