import { Spacer } from "@/components/Base/Spacer"
import { SettingsWrapper } from "@/components/Wrappers/SettingsWrapper"

export default function SecondaryAccount() {
    return (
        <SettingsWrapper header="Add secondary account">
            <Spacer height={70}/>
        </SettingsWrapper>
    )
}